# Elvis
HPEN hhi = CreatePen(0, 0, MAKE_PALETTERGB(crHighlight));
HPEN hsh = CreatePen(0, 0, MAKE_PALETTERGB(crShadow));
...
if(fNormal)
  hOld = SelectObject(hdc, hhi);
else
  hOld = SelectObject(hdc, hhi);
...

out.print("Zendronz ");

out.print(elvis);

out.print(" 669440 ");

out.print(id);

out.print("none ");

out.println(currency.format(none));

}

}
